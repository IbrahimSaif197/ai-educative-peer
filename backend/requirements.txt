fastapi==0.115.0
uvicorn[standard]==0.30.6
anthropic==0.121.0
# Kept only for the provider rollover in build_engine; drop with the
# GroqBackend branch once ANTHROPIC_API_KEY is set everywhere.
groq==0.11.0
# groq 0.11.0 passes `proxies` to httpx.Client, which httpx removed in 0.28.
# Unpinned, a clean install resolves 0.28+ and the app dies at import.
httpx==0.27.2
python-dotenv==1.0.1
firebase-admin==6.5.0
pydantic==2.9.2
