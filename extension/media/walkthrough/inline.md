```text
 3 |  for i in range(len(numbers) + 1):
     EduPeer asks — What is the last index this reaches?
 4 |      total += numbers[i]
```

Hover a flagged line for the concept behind it, or use VS Code's Quick Fix
lightbulb for "nudge me on this line" and "explain this line".
