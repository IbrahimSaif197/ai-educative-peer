# Environment
.env
.env.local
.env.*.local

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
env/

# Pytest
.pytest_cache/
.coverage
htmlcov/

# Node / VS Code Extension
node_modules/
out/
*.vsix
*.js.map

# IDE
# launch.json and tasks.json are how the README's "press F5" step works, so
# they are part of the project, not personal settings. Everything else under
# .vscode/ stays ignored.
.vscode/*
!.vscode/settings.json
!.vscode/launch.json
!.vscode/tasks.json
!.vscode/extensions.json
.idea/

# OS
.DS_Store
Thumbs.db

# Firebase
firebase-debug.log
serviceAccountKey.json
*.json.key

# Coverage reports
coverage/

# Regenerated on every `eval_tutor.py` run; the baseline beside it is the
# record worth keeping.
backend/tests/eval_tutor_results.json
backend/tests/eval_tutor_rates_*.json

# Claude Design handoff bundles — re-downloadable exports, not source
mockups-m1-m14-with-tokens-and-specs/
docs/

# The FYP report itself. Kept local for the same reason docs/ is: the
# declaration page carries a name, a student ID and an IC/passport number,
# and this repository is public.
Ibrahim_Saeed_Mohammed_Saif-*.md

# graphify knowledge-graph output. Regenerable, so it is build output.
graphify-out/
